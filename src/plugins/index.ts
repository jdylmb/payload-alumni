import { Plugin } from 'payload'

// The template's redirects / nested-docs / SEO / form-builder / search plugins
// were removed — the ACRO site doesn't use them. Add project plugins here.
export const plugins: Plugin[] = []
